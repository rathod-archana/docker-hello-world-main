name: API Build
 
on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual triggering if needed
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
 
      - name: 'Docker Login'
        uses: azure/docker-login@v1
        with:
          login-server: abc
          username: 12
          password: 134
 
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ${{ secrets.REGISTRY_LOGIN_SERVER_NEWAZ }}/dummy:latest
          file: ./Dockerfile
          context: .
